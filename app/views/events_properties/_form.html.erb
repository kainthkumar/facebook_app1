<%= form_for(@events_property) do |f| %>
  <% if @events_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@events_property.errors.count, "error") %> prohibited this events_property from being saved:</h2>

      <ul>
      <% @events_property.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
